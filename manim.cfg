[CLI]
# Default to high quality (1080p @ 60fps) for production renders
# Override with -ql for quick preview, -qk for 4K
quality = high_quality
preview = false

[renderer]
# Output goes to ./output/{scene_name}/
media_dir = ./output

[output]
# MP4 container for YouTube compatibility
format = mp4
